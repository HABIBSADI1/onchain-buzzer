{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd onchain-buzzer && npm install && npm run build"
  },
  "deploy": {
    "startCommand": "cd onchain-buzzer && npm run preview -- --host",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
