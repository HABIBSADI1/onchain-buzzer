{
  "name": "server",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "api": "node --loader ts-node/esm index.ts",
    "watcher": "node --loader ts-node/esm watchGame.ts",
    "start": "concurrently \"npm run api\" \"npm run watcher\""
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.2.1",
    "express": "^4.18.2",
    "viem": "^1.25.0"
  },
  "devDependencies": {
    "ts-node": "^10.9.1",
    "typescript": "^5.3.3",
    "concurrently": "^8.2.0"
  }
}
